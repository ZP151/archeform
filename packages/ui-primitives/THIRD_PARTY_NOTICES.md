# Third-party notices

This package contains Factory-authored registry descriptors and copyable source
snippets only. It copies no third-party source. `lucide-react` is an approved
runtime coordinate; its license remains with its installed package.
