# Resource and action declarations are compiled exclusively from the immutable
# Published Graph PolicyModel. This package establishes the guarded contract
# boundary; it neither embeds source policy nor accepts provider configuration.
