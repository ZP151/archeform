/** The legacy template identifier remains a checked guide claim, never an executable descriptor. */
export { createDefinitionEntry as createApprovalDefinition } from "./definition-family-registry.js";
