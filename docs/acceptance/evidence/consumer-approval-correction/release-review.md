# Approval correction final slice release review

RELEASE_REVIEW: PASS

Findings: P0/P1/P2 = `0/0/0`.

No release-blocking correctness, security, lifecycle, cross-package, or release-policy finding remains in the reviewed slice.

## Review boundary and source identity

- Independent final Sol reviewer: `/root/approval_correction_release`. This review followed the clean task-level recheck and successful independent Terra QA; it does not replace either gate. Only this report was written. No production source, test, dependency, Git state, service, provider, or environment was changed, and no broad suite was rerun.
- Reviewed base `92f21089beeb184a236a1512cb4c1866f158e903`, the complete frozen `review-package.diff`, and the 39-path `source-freeze.json` captured at `2026-09-12T16:18:36.240Z`. All 39 paths matched when this review began. Root subsequently made the disclosed controller-only status/checklist updates to `docs/acceptance/approval-correction.md` and `docs/superpowers/plans/2026-09-12-approval-correction.md`; production and tests did not change.
- Independently recomputed the current `docs/acceptance/evidence/consumer-approval-correction/source-identity.json`: all 28 production and TypeScript test paths used by attempt 7 match with zero mismatches. The production hashes also agree with `runtime-identity.json`, including the compiler mutation module, compiler facade/database target, adapter selectors, worker verifier sources, and presentation modules embedded in the final worker, Control Plane, and Workbench images.
- Read `AGENTS.md`, `docs/delivery-policy.md`, `docs/threat-model.md`, the consumer product checklist, the accepted ADR-0060/ADR-0062 decisions and standing ledger acceptance, the frozen release brief, implementation report, final task review and scoped rechecks, independent QA report, actual attempt-7 log, runtime identity, journey summaries, visual review manifest, and cleanup record.

## Release-risk paths checked

- Correction selection remains fail closed and checksum-bound to the separate immutable composition lock, with exact roles, grants, transitions, effects, package locks/bindings, entity/page shape, and required four-state enum storage at `packages/compiler/src/approval-mutation-contract.ts:44` and its worker-local mirror at `apps/compiler-worker/src/verifier/verification-graph-plan.ts:849`. The immutable lock enters generation and database planning at `packages/compiler/src/index.ts:3874` and `packages/compiler/src/targets/database/target.ts:1123`; the Published Graph is not hydrated or rewritten.
- The generated mutation path performs current authorization before receipt access, validates exact bodies/fields/reason/key, scopes and hashes requests, replays before record/version/state evaluation, uses conditional version writes, and commits record/audit/effects/outbox/receipt together at `packages/compiler/src/approval-mutation-contract.ts:394`. Prisma uses a `Serializable` transaction and the scoped unique receipt constraint at `packages/compiler/src/approval-mutation-contract.ts:477`. The secondary entity and legacy transition endpoint cannot reach correction mutation behavior at `packages/compiler/src/approval-mutation-contract.ts:395`, `:444`, and `:539`.
- Browser transport forwards the command key and exposes PATCH only for selected correction output at `packages/compiler/src/index.ts:4118`. Per-record command ownership, retained unknown-result payload/key, pending locks across filtered remounts, current-scope conflict feedback, escaped reasons, and role/entity guards are implemented at `packages/compiler/src/approval-mutation-contract.ts:658`. Worker request forwarding preserves validated session authority plus the bounded idempotency header at `apps/compiler-worker/src/verifier/probes.ts:77` and enforces the bounded header/body envelope at `apps/compiler-worker/src/verifier/verification-environment.ts:84`.
- Focused regression source proves exact old Expense/Purchase/Booking artifact bytes, selector negatives, same-record correction, replay and differing-hash races, conflict precedence, reason validation, transaction rollback at each persistence/effect boundary, immutable returned reads, current authorization before replay, emitted API/UI typechecking, and filtered-row command lifecycles in `packages/compiler/test/approval-correction-runtime.test.ts:59`, `:182`, `:470`, `:543`, `:629`, and `:1244`. Worker tests cover the exact Published-shape lock, stored-success verification, authority-header rejection, and bounded request transport.

## Executed and visual evidence checked

- Independent QA's final command completed the two existing Playwright lanes with one worker and zero retries: `2 passed (8.0m)`. The safe attempt-7 log records both Expense and Purchase ending on the same record ID at `approved`, version `7`, with two retained decisions and eight audit events. Both lanes exercised real local PostgreSQL and generated HTTP/browser behavior, committed Create replay after an API restart, committed Return replay, competing API writes with one commit, stale UI conflict refresh, duplicate-activation suppression, denied submitted edits, role/history isolation, reload, and unchanged immutable Compilation identity.
- The final evidence tree contains 48 correction-state PNGs and 29 baseline PNGs. I directly inspected representative cross-family edit, Return-reason, returned, pending, conflict, Create-retry, Return-retry, and approved-history images across 390/768/1440 widths. They retain the approved hierarchy and local media, readable business values/reasons, visible recovery and disabled states, unambiguous controls, and no observed clipping or overlap. Root's 12-image manifest and independent QA's remaining 36 correction-state review report no visual findings; the executed lanes also report loaded CSS/icons/media, no body overflow, 44 px controls, keyboard Details behavior, and zero applicable axe violations.
- `cleanup.json` records both final PreviewRuns as persisted `stopped` and zero containers, networks, and volumes for the exact Factory project and all ten historical/final Preview projects. The evidence scan found no populated credential value or retained raw model material; the Compose evidence explicitly leaves provider keys empty and the final journeys record `modelCalls: 0`.

## Residual scope

This verdict covers the accepted local, provider-free Expense and Purchase correction slice. It does not establish real-model quality, verified person identity, requester ownership/private records, tenant isolation, multi-stage approval, external-provider reconciliation, managed hosting, cloud deployment, or repository-release readiness. Those remain outside the accepted slice and are not waived by this PASS. Controller acceptance reconciliation, commit, push, and any later integration/repository release remain subject to `docs/delivery-policy.md` at their exact resulting commits.
