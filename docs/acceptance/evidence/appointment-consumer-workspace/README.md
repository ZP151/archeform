# Appointment consumer workspace evidence

Status: Tasks 1, 2 and 2b source pass independent source review and deterministic QA.
Task 3 source/component acceptance closes at 0/0/0 after the same reviewer's
[two scoped corrections](task-3-review-correction.md). The full 31-case run and
final three-case pagination/type recheck pass. Task 4 integration is active;
replacement default and
actual consumer acceptance remain pending. This is a repair of one logical
product, with no added runtime family or logical coverage claim.

Accepted ADR-0081 SHA-256:
`65f867c7afeeefc892b8493e508d86d5d212302fcc247e927843ce05b6672adc`.
Source base: `7a85d6efd946ffd9d1a826771e0e12a187a85b92`, with reviewed ADR-0079
predicate changes preserved in the pre-edit snapshot. The Task 1 manifest binds
six changed/new files; the snapshot also confirms two existing tests are unchanged.

## Task 1: exact Blueprint and composition admission

Writer `appointment_v2_admission` adds `read-availability` and public browser-safe
`isAppointmentConsumerWorkspaceBlueprint(blueprint, selectedKeys?): boolean`.
The private full structural witness is shared with retained V1; V2 changes only
the two exact ordered Schedule grants. The planner/composer derive the current
seven locks and 17 owner-aware Appointment bindings. V2 rejects altered lock
coordinates and complete reordered/rebound bindings even when integration
operations are recomputed to match a tampered plan.

Internal `productGraphBindings` moves from plan alternatives to composer to reuse
the derivation without an import cycle. It is not exported through the public
package index. V1 automatic eligibility stays unchanged; V2 remains ineligible
until its runtime and interface are implemented. Adapter data and provider schema
are unchanged; the latter has explicit verb lists to synchronize at integration.

Writer RED: the Graph parser rejects the new verb and V2 planning rejects its
permissions; the separate predicate is initially absent. Final reported checks:

| Check                                           | Result              |
| ----------------------------------------------- | ------------------- |
| Full Graph suite                                | 861/861, 24 files   |
| Full capability suite                           | 507/507, 37 files   |
| New V2 focused suite                            | 31/31               |
| Compiler definition-data compatibility          | 77/77               |
| Graph/capability types and builds               | Pass                |
| Changed-file formatting and whitespace          | Pass                |
| Public predicate browser esbuild, `write:false` | Pass, 450,197 bytes |

Independent `appointment_v2_admission_review` reviews the delta against
`generated/.appointment-v2-task1-base-7a85d6ef/task-1-review.diff`.
Independent Terra `appointment_v2_admission_qa` runs affected focused tests,
typechecks and historical definition compatibility against the frozen source.
Source review now reports spec/quality approval, P0/P1/P2 0/0/0. It verifies
all six current hashes and seven baseline hashes, complete V1/V2 structural
checks, tamper rejection and unchanged consumer admission. The binding-helper
relocation is text-identical apart from its name/export and remains private to
the package. Independent Terra QA now also closes 0/0/0, with ADR/source hashes
unchanged before/after, Graph focused 48/48, capability admission/planner/composer
94/94, compiler historical compatibility 77/77 and both typechecks passing.
This accepts the bounded source-admission task, not a working V2 consumer app.

## Task 2: compiler reads

`appointment_v2_compiler_reads` receives the serialized seven-path compiler
assignment in the ledger. The exact private witness, bounded store queries,
availability/summary endpoints, authorization and live-clock proxy integration
must preserve old generated bytes and existing mutation behavior. Root captures
three existing files and four absent new paths in
`generated/.appointment-v2-task2-base-7a85d6ef/manifest.json`. The writer now freezes
all seven paths; root verifies the baseline copies and captures the delta at
`generated/.appointment-v2-task2-base-7a85d6ef/task-2-review.diff`. The
`task-2-source-manifest.json` now records the corrected frozen source. Independent
review's one P2 is addressed as recorded below; scoped re-review closes 0/0/0.
Independent Terra QA now also closes 0/0/0; no runtime acceptance follows from this freeze.

Writer RED begins at immutable numeric admission. Final checks, all exit 0:

```text
pnpm --filter @factory/compiler test -- test/appointment-consumer-compilation.test.ts test/appointment-consumer-read.test.ts test/appointment-booking-runtime.test.ts test/definition-data-compatibility.test.ts
160/160: 32 compilation, 38 reads, 12 existing runtime, 78 compatibility
pnpm --filter @factory/compiler typecheck
pnpm --filter @factory/compiler build
pnpm exec prettier --check <the seven assigned paths>
git diff --check -- packages/compiler
```

The compatibility set retains all historical 77 cases and adds V1 endpoint/Date
absence. Read tests execute emitted runtime/controller/proxy modules, mocked
Prisma queries, and strict TypeScript checks of the emitted API and proxy using
Nest/Prisma declaration stubs. They do not prove actual database or browser
behavior. The final focused correction makes the Prisma import replacement
fail closed on missing/duplicate markers and adds its regression. Mutation
renderer source, capability assets and protected fixtures remain unchanged.

Independent `appointment_v2_reads_review` returns spec/quality changes required,
P0/P1/P2 0/0/1. The in-memory index scans and sorts all schedules before taking
500; its 2,000-row reproduction performs 10,262 timestamp parses. API-005 bounds
store-side candidate work, not only output length. The same writer receives a
two-file correction with visited-record regression and index-update/rollback
checks. Root preserves the first frozen seven files in
`generated/.appointment-v2-task2-review-base-7a85d6ef/manifest.json`; the same
reviewer inspects only the repair delta before independent QA.

Fix round 1 reproduces RED with 6,000 visited records for 500 returned rows.
The corrected global/service indexes are maintained on writes; queries seek
date ranges and offsets before reading at most 500 records, or zero for an
unknown service. New tests cover actual visits, ordering, service/time/invalid
start updates, returned-record isolation and transaction commit/rollback.
Writer's affected run passes 163/163 (41 reads, 32 compilation, 12 existing
commands, 78 compatibility), compiler types/build and two-file format/whitespace.
Root verifies the two new hashes and the other five unchanged paths against the
preserved baseline and creates `fix-1-review.diff`. The same reviewer confirms
P2 addressed, spec/quality approved and P0/P1/P2 0/0/0. Independent
`appointment_v2_reads_qa` now runs the four focused suites and typecheck against
that frozen manifest. A role-based dispatch failed unavailable Spark resolution
without creating a child; the explicit Terra dispatch is the sole QA owner.

Terra reproduces all four suites, 163/163, and compiler typecheck with ADR and
all seven source hashes matching before/after. Its test-relevance review covers
bounded indexes/Prisma query shape, auth/IDs, Date/proxy recovery, pagination,
historical summaries, V1 compatibility and emitted types. PM accepts this
bounded source task for the serialized presentation handoff. Actual PostgreSQL,
framework routing, responsive browser and consumer acceptance remain pending.

### Frozen private interfaces for Task 3

- `selectAppointmentConsumerProfile(graph, compositionLock)` returns
  `{ key: "appointment-booking@2.0.0", runtime, serviceName, serviceDuration }`,
  undefined for historical profiles, and throws for unsupported availability
  witnesses. The compiler stores it as `appointmentConsumer`; both actual
  compilation seams apply the V2 witness.
- `renderAppointmentConsumerFile(path, source, profile?)` wraps V2 runtime,
  store, controller and proxy output. `renderAppointmentConsumerRead(profile)`
  emits the private API read module. No public package subpath is introduced.
- Generated runtime methods are `appointmentAvailability(role, rawUrl, now)`
  and `appointmentSummary(role, recordId)`. Store additions are
  `scanAppointmentSchedules(query)`, `countAppointmentOccupancy(ids)` and
  `latestAppointmentSlot(id)`; scan/page bounds are 500/100.
- The interface must bootstrap from authenticated Appointment-list `Date` and
  implement the accepted retained-window and selection recovery. Missing
  upstream Date is forwarded as empty to prevent substitution by the proxy
  host's clock; empty/missing/invalid values require recovery. No client clock
  fallback is permitted.
- A V2 pre-routing summary guard authenticates before malformed path decoding;
  old booking envelopes, receipt/history behavior and defaults remain unchanged.

## Task 2b: administrator setup prerequisite

Accepted ADR-0082 SHA-256 is
`5734bacdd01d4b27475ea861ed94f97011191a587eddfe73ad68e8900473a7d9`.
The writer freezes four changed files and one unchanged owned read module in
[the source manifest](task-2b-source-manifest.json). Root verifies those hashes,
the three preserved pre-edit copies and the final report/delta hashes. Review
uses the scoped diff under `generated/.appointment-v2-setup-base-7a85d6ef/`,
not a HEAD diff containing earlier tasks. `appointment_v2_setup_review` owns
the independent source review; its correction and independent QA close below.

The first independent review returns P0/P1/P2 **0/0/1**. Real default JSON
parsing collapses repeated field/envelope names before exactly-once validation;
its focused non-listening reproduction accepts duplicate Service and PATCH
members and produces writes. Fix round 1 is assigned to the same writer for
setup-scoped duplicate detection and regressions, preserving authorization-first,
safe errors and ordinary-route parsing. The first five-file snapshot and reports
remain under `generated/.appointment-v2-setup-review-base-7a85d6ef/`; the current
tracked manifest records that reviewed pre-fix snapshot until a new freeze.
The same reviewer will recheck only the correction and resulting boundary effects.

That scoped recheck now closes P0/P1/P2 **0/0/0**, spec/quality pass. Fix-round-one
source is recorded in the updated manifest; the writer's final five-suite result
is **240/240** with 77 setup cases. Native-parser parity includes actual supported
charsets, MIME/strict JSON admission, size and compression; ordinary requests
retain their parser behavior. Only emitted `api/src/main.ts` differs from the
first reviewed snapshot. Explicit Terra `appointment_v2_setup_qa` reproduces
240/240 checks (77 setup, 41 reads, 32 compilation, 12 booking, 78 compatibility)
and compiler typecheck, with ADR and all five source hashes unchanged before
and after testing. P0/P1/P2 is 0/0/0. The combined output reached the host time
boundary, so QA also obtains an authoritative exit-zero result for each file
individually. PM accepts this bounded source checkpoint. Actual acceptance stays
open; this is neither a hosted rollout nor proof of PostgreSQL persistence.

Writer's final affected checks pass **230/230**: 67 setup, 41 reads, 32 compilation,
12 booking and 78 definition compatibility. Compiler typecheck/build, strict
emitted API/store/proxy types, formatting and whitespace pass. Tests start RED on
missing runtime/route/proxy/ordering behavior, catch a malformed update being
interpreted as create, and catch case-insensitive route-prefix parser bypass.
The final emitted comparison proves only the five accepted paths change, all
60 other files and old file order remain identical, and all 13 expected original
booking/controller/Prisma methods exist and remain byte-identical.

The new private setup module owns current-reference counts; the existing read
module is byte-identical to its accepted snapshot. Strict setup validation,
expected-value updates, atomic audit/isolated receipts and replay reuse existing
transactions. History-only references do not lock a slot. The read-test change
only updates obsolete Nest mocks/types for the accepted endpoint/bootstrap.

Bootstrap tests run emitted registration with actual Express parser middleware
and non-listening dependencies. Prisma tests use driver doubles for predicates,
retry and retained-state replay. Neither establishes actual Nest HTTP behavior,
PostgreSQL contention/persistence, UI recovery, hosting or product acceptance.
The actual cross-role browser and database case remains required in Task 5.

## Task 3: consumer presentation

**Current checkpoint:** root completes the four-path implementation after the
initial handoff. All 23 integrated emitted-browser/type cases pass, with the
affected 133-case lane and final compiler types/build recorded in
[the implementation report](task-3-implementation.md). The
[source manifest](task-3-source-manifest.json) is frozen for one independent UI
review. Actual Preview, automatic entry and hosted delivery remain open. The
paragraphs below preserve the earlier prerequisite and preparation history.

`appointment_v2_workspace` receives the serialized four-path assignment after
Task 2 review/QA close. Root revalidates the two existing files and two absent
new paths in `generated/.appointment-v2-task3-base-7a85d6ef/manifest.json` before
dispatch. The [reuse/interaction handoff](ui-reuse-handoff.md) and ignored Task 3
brief bind the customer, staff and administrator behavior, clock recovery,
uncertain-command rules and responsive evidence. No implementation result is
reported yet. Synthetic captures/report stay under the ignored Task 3 directory
until root inspection; actual PostgreSQL/Preview acceptance remains later.

Task 3 preflight now confirms a missing prerequisite, before source edits.
The actual emitted V2 controller has no PATCH/PUT setup route, and its
`ApplicationRuntime` has no update method, although administrator Graph grants
declare Service/Schedule update/manage and the store has an update primitive.
The planned setup-edit controls cannot use an existing authenticated API as
assumed. The ignored characterization test passes 1/1 using actual generated
source; [safe route/permission metadata and source digests](setup-update-gap.json)
retain the evidence. Both existing Task 3 files match baseline and both new
source paths remain absent; no UI test or screenshot has been produced.

PM pauses Task 3 source mutation and dispatches proposal-only ADR-0082 to
`appointment_admin_update_decision`. It must resolve the narrow setup-write
contract before implementation, preserve accepted ADR-0081 bytes and old
booking behavior, and avoid a create-only substitute. The accepted query/source
tests remain valid within their scope; they never established setup editing.

ADR-0082 is now accepted at SHA-256
`5734bacdd01d4b27475ea861ed94f97011191a587eddfe73ad68e8900473a7d9`.
Independent review initially rejected permanent history-only slot locking; the
revised proposal protects current references while preserving immutable history
snapshots. Its scoped recheck returns standing acceptance yes, P0/P1/P2 0/0/0,
and PM records acceptance before implementation. `appointment_v2_setup_runtime`
owns the compiler paths in the ledger; its frozen source and writer results are
recorded above, pending independent review and QA.

The Task 2 manifest records the independently accepted source snapshot. Task 2b
owns the next narrow `index.ts` and store-hook integration, so those prior hashes
become historical when changed; their exact bytes remain in
`generated/.appointment-v2-setup-base-7a85d6ef/`. Task 3 needs a fresh baseline
after that prerequisite. Its original baseline remains preserved.

The later ledger scheduling adjustment permits disjoint UI-module/test/helper
preparation while setup QA completes; facade integration stays serialized.
Initial UI-worker dispatches hit the tool's task limit, so root first implements
a partial foundation locally. Two real emitted React/runtime tests go RED then
GREEN: exact phone booking and missing-server-Date recovery. This does not cover
the remaining admin/staff/cancellation/history/conflict/visual requirements.
The partial snapshot is `generated/.appointment-v2-task3-local-prep/manifest.json`.
After the setup writer completes, fresh `appointment_v2_workspace_prepare`
successfully receives the same three paths to finish the complete accepted scope;
root stops editing those source paths. No facade/default or product-count change
is made by this preparation handoff.
The read interfaces and other Task 2 source are frozen for this handoff.

After independent setup QA closes, PM grants the same UI writer serialized
`index.ts` presentation/style/manifest integration. The fresh accepted setup
facade baseline is preserved at
`generated/.appointment-v2-task3-facade-base-7a85d6ef/manifest.json` with SHA-256
`2a782be0bb8505aa089ac314b434f59e154679724b19cf2e4713cef1238099ef`.
The earlier Task 3 baseline remains historical. Setup/read/booking behavior and
adapter defaults stay frozen. Complete UI and actual acceptance remain open.

Root inspects the three first component captures under
`generated/.appointment-v2-task3-ui/attempt-20260924-ui-001/` while implementation
continues. This is a failed intermediate visual result: the 390 px customer main
content occupies a narrow left column beside the success alert, wrapping ordinary
words and overflowing controls; desktop staff cards expose every correction form
at once with excessive height, and administrator service controls stretch to the
taller adjacent schedule form. These observations return to the same writer for
the already planned bounded visual correction. Preserve the failed captures and
add actual main/form width and overflow checks. The images are neither actual
Preview evidence nor final UI acceptance. Later source edits require new captures
before claiming these visible problems resolved.

Root prepares the [Task 3 reuse and interaction handoff](ui-reuse-handoff.md)
while the compiler writer continues. The ordered search confirms existing native
controls, token styles and pinned icons, and identifies a concrete integration
gap: the shared collection hook discards the response Date needed for V2 clock
bootstrap. The handoff maps emitted-control and actual journey checks without
adding an acceptance stage. No presentation source or runtime is claimed yet.

## Remaining product outcome

The bounded availability/summary protocols, exact compiler witness and setup
updates now have source acceptance. Complete the actual customer/staff/administrator
controls and their facade integration. Only after that
integration is ready should fresh selection and automatic delivery use V2.
Preserve old V1 artifacts and label shared fixture identities honestly.

Root prepares Task 4 regression in the disjoint new adapter test
`appointment-consumer-definition.test.ts`. Its first real run executes six cases:
historical explicit projection passes; five expected RED cases identify missing
V2 registration/provider offering/projection and continued V1 automatic admission.
These tests are deliberately unfinished Task 4 work, not an accepted green lane.
No adapter or matcher production source and no consumer defaults change. The
ignored preparation brief records the narrow subsequent implementation handoff.

The existing local startup rejection remains unresolved; no alternate startup is
authorized by this source work. Actual UI, persisted cross-role business actions,
responsive inspection and cleanup remain missing evidence. API-only operations,
emitted metadata, unit tests and screenshots without asserted interaction do not
establish ordinary-user completion. Hosted delivery remains a separate outcome.
