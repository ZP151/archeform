# Customer Requests verifier source acceptance

PM accepts the six frozen worker source/test paths on 2026-09-24 after independent
task review, scoped repair closure, Terra QA and Sol final source judgment.
Open P0/P1/P2 findings are 0/0/0. This completes Task 3 source work together with
the separately accepted responsive presentation; actual product acceptance remains
open.

The worker selects the exact immutable Customer Requests profile and verifies
the complete conversation through its existing private journey seam. Its bounded
62-request sequence checks creation, metadata and reply correction, customer/staff
replies, resolution, reopen, cancellation, same-role owner isolation and original
receipt replay after later changes. Canonical, renamed, swapped and maximum-length
role variants use the same fixed principal identities. Generic probe bodies and
top-level ID capture retain their existing behavior.

The private adapter validates exact request descriptors before transport and
bounded response data before capturing transient IDs or comparison digests.
Review exposed premature accessor reads/inherited fixture acceptance and negative
zero response versions. Focused failing cases reproduced both; the repaired own
data/prototype guard and scalar comparison close them without changing generated
runtime, API, lifecycle or persisted verification contracts. Evidence contains
fixed safe summaries, not conversation content or transient identifiers.

Fresh independent QA passes 141 family cases, 100 generic environment/probe cases,
worker typecheck and twelve protected historical-plan comparisons. Root's
post-QA worker build passes. The repair's raw RED/GREEN records retain 22 failed
and four passing controls before fixes, then 31 selected passing cases. Earlier
274 historical tests and compiler output parity remain evidence for unchanged
scope; they are not described as fresh full-suite runs.

Portable evidence:

- `task3-verifier-source-manifest.json`: six accepted source identities and the
  three changed identities relative to the original freeze.
- `task3-verifier-review.md` and `task3-verifier-recheck.md`: original findings
  and scoped closure.
- `task3-verifier-qa.json` and `task3-verifier-historical-plans.json`: independent
  executable results and unchanged twelve-plan baseline.
- `task3-verifier-judgment.json`: separate final source judgment.

Original repair manifest SHA-256 is
`28e2629bb4449612476513fe08e69bff3056e1a4e12413af12994bb9730a0d0e`;
original final judgment Markdown SHA-256 is
`3abbee2bca79ef6009c57d2ab94f956ec0e007623cfc441d443fe0b108d024b1`.
Original reports, raw repair logs and before copies remain under
`generated/.customer-requests-task3/verification/` and the independent verifier
review/QA/judgment directories. Portable copies are formatted for the repository;
original receipt hashes refer to the original retained files.

The exercised runtime is emitted code with an in-memory store and substituted
transport/process hooks. No application listener, PostgreSQL service or hosted
environment ran. This acceptance establishes neither actual persistence/races,
restart, consumer browser delivery, private authentication, hosted availability,
upgrade/rollback nor repository release. Registered, actual-local and hosted
counts do not increase. Task 4 definition, automatic consumer entry and actual
journey evidence are subsequent work.
