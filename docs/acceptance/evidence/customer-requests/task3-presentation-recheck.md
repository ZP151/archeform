# Customer Requests presentation repair 1 — scoped recheck

Date: 2026-09-24. Verdict: **accept the two-file repair for the three original P2 findings**. P0: none. P1: none. P2: none remaining within this recheck. This is source and emitted-component closure only, not actual generated-product or hosted acceptance.

The repair manifest `generated/.customer-requests-task3/repair-1/source-manifest.json` hashes to `9c2b9a5fdaa815dfff2f7b8aa2bc03adc2f43909ef6cccfb83fb952f7619d829`. Its current source hashes match the filesystem: `customer-requests-presentation.ts` `a48c4acd8c490acd9c2baac2788d505f327e4f1169f6d0a566c5f3cf5e9d9a69`; `customer-requests-presentation.test.ts` `7cc065243d86424ab23e0d9a74e183799cbdc0ff6dcf7276f2cf1550d9ded211`. The preserved before copies match the original reviewed hashes `b6497e2e...955a853` and `8f5c6310...efb9`. The root compiler index and shared style hashes remain `2161d140...b411da90` and `36766331...932e2bd6`. All five evidence/report hashes in the repair manifest match the retained files.

1. **History boundary closed.** At `packages/compiler/src/customer-requests-presentation.ts:150-168`, every detail reload aborts the earlier-history fetch, advances `historyEpoch`, resets paging and loads a fresh first page. `:213-222` accepts an earlier page only while its controller is live and its epoch/request ID remain current. The focused test at `packages/compiler/test/customer-requests-presentation.test.ts:332-410` holds an old `beforeVersion=3` response, saves a newer reply, refreshes the same detail, observes the old request abort, then pages using the new cursor and finds the formerly skipped event. This directly closes the reported interleaving.
2. **Principal read cancellation closed.** At `packages/compiler/src/customer-requests-presentation.ts:127-168`, list, detail/first-history and earlier-history fetches use abort signals; superseding loads and component unmount abort their pending controllers while the epoch/alive checks remain. Tests at `packages/compiler/test/customer-requests-presentation.test.ts:411-541` hold and observe failed GETs for list, detail and earlier history after switching principals, and verify the new principal's view is clear. The command fetch and exact uncertain retry remain on their prior path.
3. **Focus restoration closed.** At `packages/compiler/src/customer-requests-presentation.ts:169-181`, route and editor transitions record the originating control and focus the mobile Back control, form entry, editor field or return target as appropriate. The test at `packages/compiler/test/customer-requests-presentation.test.ts:542-623` drives Enter across list/detail, form and reply-editor transitions and asserts the exact active element after each forward/return step. This is materially stronger than the earlier generic Tab assertion.

The retained RED log has the three intended pre-fix failures (history cursor, missing abort, missing focus). The GREEN log records 19/19 emitted-component cases; compiler typecheck and Prettier logs are green. I did not rerun these unaffected or already passing checks. The original 91-case compiler integration, 12/12 protected historical equality and visually inspected emitted-page captures remain valid for their prior scope. The verifier/ADR-0085 implementation and actual service, database, startup, provider, cloud, cleanup and Git operations were outside this recheck.
