# Customer Support Desk definition and consumer source acceptance

PM accepts the 25 frozen Task 4 source paths on 2026-09-24 after independent
combined review, Terra QA and Sol final judgment, all with P0/P1/P2 0/0/0.
This accepts source registration and the authored actual-case source. Actual
product acceptance remains pending.

`customer-support-desk@1.0.0` uses the accepted `customer-requests/v1` family,
the same six physical capability locks, runtime and responsive presentation.
Customers submit, correct, reply, read resolutions, reopen and cancel from a
phone conversation; staff triage, reply and resolve from the desktop queue.
Definition data supplies the exact Graph-backed contract. No new runtime or UI
branch is introduced for the definition.

The consumer entry recognizes the exact family structure, locks and bindings
through the existing shared matcher. Existing phase latches select, apply,
Publish, compile, verify and open the application without manual technical
steps in the source harness. Material questions, manual opt-out, readiness,
failure recovery and immutable creation identity retain their existing checks.
Private accounts, uploads, external intake/messages, assignment, internal notes
and SLA requirements remain explicit unsupported scope, rather than being
silently removed from a request. Authored provider results establish this
retention behavior, not live-model interpretation accuracy.

Fresh independent QA passes 61 definition, 46 planning, 218 consumer and 25 pure
helper cases: 350 total. E2E types pass, all 25 source hashes match, and twelve
current historical derivations and immutable emitted bundles remain identical.
The original twelve catalogue rows and source prefix are preserved. Writer's
broader 419 adapter cases, affected package types/builds, root's successful
eight-step definition lane and full-repository formatting are retained. Initial
RED and stale-build-output failures remain in the local evidence packet.

The authored browser case covers Home entry, customer/staff conversation and
corrections, repeated resolution/reopen, cancellation, same-role owner denial,
uncertain-write retry, stale correction, principal-switch cache clearing,
keyboard/touch and 390/768/1440 states. It records immutable source identities,
safe digests, ready/first-useful-action times and technical handoffs. Its helpers
validate exact resource ownership and fail closed on uncertain cleanup. One
actual case is discovered only; global discovery finds 98 cases in 39 files.

Portable evidence includes `task4-source-manifest.json`,
`task4-source-review.json`, `task4-source-qa.json`, `task4-source-judgment.json`,
`task4-definition-lane.json` and both historical-equality receipts. Original
definition/consumer manifest SHA-256 is
`01dfd92eb93092500d8f4978b2904a21d5465079697f13fa3dd54dc66777a66b`;
case supplement SHA-256 is
`e261e18bce74b2de9d616eeeea83e15206cbf03815b3f86963296b3617f6de50`.
Original final-judgment JSON SHA-256 is
`a6276e581d739f57432262503d3168f22b89ab09c0e0344cf049bb9479d3cf06`.
Original logs and reports remain under `generated/.customer-requests-task4/`
and its separate review/QA/judgment directories. Portable JSON copies are
formatted; receipt hashes refer to their original retained files.

The catalogue now registers thirteen physical rows, twelve logical definitions
and eight families. The historically delivered baseline remains ten definitions
across six demonstrated families, with the existing Appointment UI qualification;
hosted products remain zero. No actual service, PostgreSQL, provider, deployment
or cleanup ran for this slice. Actual cross-device image inspection, persistence,
races, restart, retained-data upgrade and recovery remain open. The execution
guard does not clear the existing rejected startup boundary. Hosted delivery
also needs its concrete environment, access/data/operator scope and accepted
provider decision. This source acceptance grants no main integration or release.
