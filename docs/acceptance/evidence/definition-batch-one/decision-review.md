# ADR-0066 Independent Standing-Acceptance Review

Date: 2026-09-13.

- Reviewer: `/root/generic_identity_decision_review` (independent read-only reviewer; neither the proposing Tech Lead nor an implementation writer).
- Proposal: `docs/adr/adr-0066-generic-approval-record-identity-and-summary.md`.
- Exact SHA-256: `d49e33da56a46e9d5a87856ff46e8d21ea61c20104cf311758727f140abc1048`.
- `APPROVED_FOR_STANDING_ACCEPTANCE: yes`.
- P0/P1/P2: `0/0/0`.
- No release-blocking proposal finding exists.

The review checked `AGENTS.md`; the founder standing authorization and ADR
decision gate in `docs/tech-governance.md`; `docs/threat-model.md`; the active
ledger; the batch-one design and plan; accepted ADR-0059, ADR-0060, ADR-0061
and ADR-0065 boundaries; current manifests and exact lockfile resolutions; the
actual compiler selection, list, history, correction, presentation and media
paths; the original-four compatibility fixture; and the retained attempt-one
facts, log and 390/768/1440 screenshots. The screenshots and facts independently
show zero visible card titles and summaries, while the generated workspace,
family hero, status and controls remain present. The compiler production paths
have no source diff at this decision gate.

The current correction selector fails closed on the exact role, state,
transition, grant, page, composition-lock, provider and reserved-field shape in
`packages/compiler/src/approval-mutation-contract.ts:45-299`. Current list
emission derives only the legacy summary/title heuristics in
`packages/compiler/src/index.ts:2719-2736` and
`packages/compiler/src/index.ts:2859-2878`; current history independently repeats
the `item`/legacy-summary fallback in
`packages/compiler/src/approval-decision-history.ts:37-55`. The proposal places
one compiler-derived `approval-record-identity/v1` projection behind the exact
correction selector, excludes reserved fields, requires one unambiguous Graph
`string` title, limits summaries to the first two declared non-status enums, and
requires list and matched history to consume the same projection. Zero or
multiple title candidates stay on the existing safe fallback and cannot pass
generic presentation admission.

The proposal preserves the public Graph/API/data/schema and generated mutation
contracts, immutable Draft -> Published Graph -> Compilation lifecycle, server
authorization, history scope invalidation, original Expense/Purchase identity
and media paths, all original-four ordered bundle bytes, dependencies, assets,
CSS, services and topology. Its optional Publication row-media assertion matches
ADR-0061 EXP-001/EXP-002, which gives unknown signatures a generic family hero
and no record-tile photo. Verification requires focused ambiguity, reserved-key,
runtime-value and determinism cases; exact original-four compatibility; emitted
source boundary scans; the actual two-record list/history correction journey;
role denial, stale/retry behavior, responsive/accessibility evidence; and exact
cleanup.

The decision is bounded, reversible and inside the active founder-approved goal
of gradual definition growth with a complete approved Approval journey. It
introduces only private conditional presentation versions
`approval-workspace-presentation@2.2.0` and
`approval-decision-history@1.1.0` for previously unsupported eligible generic
correction Graphs. It leaves ambiguous identities unadmitted and grants no
Product Publish, repository release, external provider, paid resource, cloud,
deployment, credential, security-boundary weakening, destructive migration or
irreversible authority. No material product or business choice remains open.

PM must record this exact digest, reviewer, verdict, evidence and the founder's
2026-09-01 standing authorization before assigning source implementation. This
decision review does not approve an implementation diff or claim post-change
runtime success; the applicable proportionate implementation, QA/review and
controller-delivery checks remain required after implementation. Only this
review receipt was written.
