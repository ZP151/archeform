# Definition batch one independent implementation and product review

Date: 2026-09-13.
Reviewer: `/root/definition_batch_acceptance_astra` (Astra; independent of the
definition, compiler and acceptance-test implementation owners).
Base: `327eb9086ff79f521e87e4db072120763b95b3e5`.

**Verdict: PASS for the bounded local Publication Review admission and accepted
ADR-0066 generic Approval presentation correction. Open in-scope P0/P1/P2:
0/0/0.** This is the one proportionate independent implementation/product review;
it does not authorize main integration, repository release or cloud deployment.

## Scope and source review

Reviewed AGENTS.md, delivery policy, the consumer product checklist, threat model,
active PM ledger, batch design/plan, accepted ADR-0066 and its exact standing
decision receipt. Inspected the actual diff, the existing correction selector,
generated list/history call paths, catalogue data, compatibility fixture changes,
public interpreter tests, built CLI checks, Playwright loader repair and final
browser helpers. No production file, Git state, service or provider was changed
by this reviewer; only this receipt was written.

The fifth definition supplies a distinct title/body/channel editorial job through
the existing Approval family. The shared compiler correction begins only after
the exact correction selector has established the frozen family shape. It
requires one required Graph string outside reserved fields and the incumbent
identity paths, selects at most two enums in declaration order, and emits one
frozen entity/title/summary projection for both cards and matched history.
Ambiguous identity remains on the old fallback. The generic fragment uses safe
value formatting and React escaping; long text remains in Details. No
Publication-specific source branch, Graph/API/mutation contract, authorization,
dependency, asset, CSS or runtime topology change was found.

The native ESM exclusions affect only test transformation of built workspace
packages. The historical compatibility helper now selects the original four
keys explicitly without replacing their stored expectations. Independent
comparison found all four existing catalogue entries unchanged against the
recorded base. Full-suite evidence confirms their complete ordered Published
bundles remain exact.

## Verification evidence

- Reviewed compiler full-suite log: 48 files and 776 tests pass, including the
  12 executable generic identity cases and original-four compatibility. Cases
  cover missing/ambiguous titles, enum order/count, renamed fields, entity scope,
  reserved fields, malformed runtime values, unmatched history, escaping and
  deterministic output. Compiler typecheck/build/lint passes are recorded by
  the implementation owner and controller.
- Reviewed adapters full-suite log: 12 files and 223 tests pass. The built CLI
  receipt covers 16 cases; current shipped validation is 5/5/5/5. Authored
  interpreter cases retain explicit publishing/media exclusions and prove
  projection, not model classification accuracy.
- Reviewed [final actual run](e2e-attempt5.log): one test passes in 4.3 minutes;
  usable local app at 191,368 ms, within the declared prepared-local target.
  Compilation `cmtzswgoo006yo255vft4jnfo`, Preview
  `preview-7534db51-8288-4e9a-9437-534051fbcc4b`; observed lifecycle is Publish,
  compile, verify, Preview. Persisted final verification succeeded.
- [Correction facts](publication-review/correction-journey.json) and the tested
  assertions prove real create, lost-response recovery after API restart,
  draft editing, concurrent writes committing once, stale edit rejection,
  submit, return with required reason, same-record correction/resubmission,
  approval and reload. The record finishes Approved at version 7 with two
  retained decisions and eight audit events. Native/API required and enum
  rejection, auditor mutation denial, and submitted/approved edit denial pass.
  A second authored newsletter record is created and approved separately.
- Recomputed all 11 current source hashes against the final
  [runtime identity](runtime-identity.json); every hash matches. The worker image
  is `sha256:2ee7393f9ae12b4b1edc8ecb1de6c279fd41ae0a67f1390bf143b8a881a20999`.
  The receipt also binds the unchanged Workbench/Control Plane images, copied
  catalogue and disabled provider key.
- Inspected [persisted runs](runtime-runs.json) and [cleanup](cleanup.json): all
  four Preview rows are stopped; the exact Factory, diagnostic, four Preview
  and four verifier projects have zero containers, networks and volumes.

## Actual visual and product acceptance

Personally inspected 22 final screenshots under `publication-review/`:

- `primary-{390,768,1440}.png`;
- `correction-draft-edit-{390,768,1440}.png`;
- `two-authored-records-{390,768,1440}.png`;
- `two-authored-history-{390,768,1440}.png`;
- `workspace-dark-{390,768,1440}.png`;
- phone `correction-create-retry`, `correction-conflict`, `correction-pending`,
  `correction-return-reason`, `correction-returned` and `media-fallback` images;
- `correction-approved-history-768.png`.

Also inspected the rejected before-correction 390/1440 images and attempt 4's
primary phone/failure images. Those show the original missing identity and the
later expanded-history test-state mismatch; they are not counted as final passes.

The final cards visibly distinguish the editorial correction article on `blog`
from the second article on `newsletter`; all three matched decision rows display
the corresponding identity/channel, action, actor and time. The return reason
is readable before the author's Edit action. Input controls retain values and
visible focus; pending actions disable appropriately, and retry/conflict screens
explain recovery. Details keep technical IDs and longer content secondary.

The existing cobalt navigation, clear action words, recognizable icons, status
badges and progress remain coherent at phone/tablet/desktop widths. The primary
Submit action is reachable in the first viewport. Expanded history intentionally
adds document height; it does not overlap or clip the record list. The visible
image-disabled fallback preserves the hero footprint and business text, records,
status and controls. Dark screens remain readable. The actual run additionally
passes loaded-style/icon detection, missing-asset detector checks, no document
overflow, relevant 44 px targets, keyboard actions and axe checks.

Functional, mechanical responsive/accessibility, and qualitative visual outcomes
pass for the supported local journey. The original four products reuse their
unchanged runtime/UI evidence through exact emitted bytes. This is independent
agent assessment, not founder visual approval or ordinary-user research.

## Limits and retained failures

Attempts 1-4 remain recorded: the inherited row-photo assertion and missing
identity, reused requirement ID, long-ID Prisma collision, and a viewport check
performed with history open. The final helper preserves legacy required-media
defaults, uses one short unique requirement ID per run, and checks collapsed
list density separately from expanded history. The image-disabled evidence gap
identified during this review is closed in the final run.

The overlong application-ID failure is an existing platform limitation, not
repaired by shortening this fixture. Prisma-generated Principal/Session key and
index names can truncate to the same identifier. The recorded roadmap follow-up
must address deterministic naming or validation before broad batch scale; this
review makes no arbitrary-ID compatibility claim. It does not add an in-scope
finding to the unchanged namespace generator.

Acceptance covers authored provider-free selection and the real local generated
API/database/browser workflow: zero model calls and zero ordinary-user sessions.
It establishes neither real-model classification nor physical-device usability.
Roles remain selectable demo roles with role-wide reads. External publishing,
scheduling, media storage, private identity, hosted operations and deployment are
excluded. Training, Equipment, Travel, Leave and Access retain their documented
capability gaps and are not additional admissions. The batch advances registered
definitions from four to five while runtime families remain three.
