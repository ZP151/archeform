# Work Orders definition and consumer case-source checkpoint

Date: 2026-09-24. Scope: source acceptance under accepted ADR-0080/0083 and
the existing automatic consumer orchestration. This does not establish actual
PostgreSQL execution, ordinary-user success, branch delivery or hosted availability.

## Outcome

The canonical `facilities-service-desk@1.0.0` definition selects the exact Work
Orders family, immutable composition locks and reusable presentation. The existing
consumer orchestrator handles a supported request without new technical lifecycle
controls. Material unsupported requirements remain questions instead of becoming
unimplemented promises. Desktop dispatch and phone execution share the same
business contract; local synthetic staff are not private hosted accounts.

The complete executable case is registered and included in tracked source checks.
It covers correction, assignment, mobile work, reassignment and former-assignee
denial, two resolution reports with reopen, cancellation, committed-response loss,
concurrent mutations, restart, immutable identity, responsive visuals and exact
owned-resource cleanup. These are implemented assertions, not executed outcomes.

## Verification

- Definition/consumer independent review and QA close P0/P1/P2 at 0/0/0:
  290 adapter, 42 matcher, 181 consumer and eight shared observer cases pass;
  capabilities/adapters/Workbench no-emit checks pass.
- Eleven pre-existing physical definitions retain equal fresh and immutable
  compilation output. The baseline was not regenerated.
- `pnpm regression definitions` passes all eight existing steps. Its control-plane
  Prisma client generation is a local build prerequisite, not a database run.
- Final case-source independent review and QA close P0/P1/P2 at 0/0/0. All seven
  integrated hashes remain stable, tracked consumer-case no-emit types pass,
  and the three-file CI pure-helper selection passes 18/18. Discovery lists one
  Work Orders case without executing it.
- Full `pnpm format:check` passes. Four mechanical formatting changes have
  preserved before/after identities and equal parsed syntax. Three exact immutable
  evidence JSON files retain their bytes through narrow formatter exclusions;
  no source code is exempted.
- Existing CI now includes definition tool/data/index checks, consumer-case types
  and pure helpers. Remote execution of the changed workflow is still pending;
  the older green branch run is not evidence for this uncommitted source.

Detailed definition review/QA: `generated/.work-orders-task4-definition/`.
Definition manifest SHA-256:
`8f3c9ed7f9f2c8dfb0b5aeed3b4001620c8a4e635242fc375392c7d6af98125b`.
Detailed case review/QA and regression summary: `generated/.work-orders-task4-case/`.
Final seven-path integration manifest SHA-256:
`ba0c5e31109ba9d3d2125b7a254b70443da48f8209b40d7781ea907ad67ed5fa`.
Formatting receipt: `generated/.work-orders-format-handoff/receipt.json`.

## Remaining acceptance

Run the actual consumer and PostgreSQL case only when the recorded local-startup
restriction is resolved, then inspect actual business screenshots and cleanup
evidence. Do not substitute helper tests or component screenshots for that run.
Prepare reviewed branch delivery separately from main/release eligibility.
Choose and authorize a concrete hosted pilot environment before its deployment
adapter and cloud actions; preserve the existing local upgrade/rollback evidence.

Working catalogue: twelve physical rows / eleven logical definitions. Historical
local baseline: ten definitions / six runtime families, with the documented
Appointment UI acceptance reopened. This checkpoint promotes no delivered count.
