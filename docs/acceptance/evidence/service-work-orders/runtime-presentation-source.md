# Work Orders runtime and presentation source checkpoint

Date: 2026-09-24. Scope: accepted ADR-0080 implementation, with the accepted
ADR-0083 private fixture identifier correction. This is source and emitted-module
acceptance, not actual PostgreSQL, consumer delivery or hosted acceptance.

## Accepted behavior

The public compiler emits the exact Work Orders runtime and coordinated desktop
dispatch/mobile technician presentation from immutable Published Graph/lock input.
Current assignment is checked before replay; commands use version checks and
atomic history/audit/receipt handling. Correction, reassignment, cancellation,
resolution and reopen preserve attributed history and prior reports. Generated
SQL uses the actual allocated database identifiers, including admitted long keys.

The emitted client loads its styles and local icons with license notices, preserves
explicit Graph themes and presents the phone action in the first viewport. It
restores the selected synthetic principal before business reads. Drafts survive
conflicts and definitive rejections; pending writes freeze staff/navigation, and
ambiguous responses retain the exact command for recovery without a new key.
The local demo does not provide private staff accounts, uploads or notifications.

## Evidence and corrections

- Independent source review covers the runtime/public integration and final
  presentation together. The same reviewer closes three actionable recovery
  findings: discarded ambiguous commands, unlocked in-flight navigation and
  refresh-cleared create feedback. Open P0/P1/P2: 0/0/0.
- Independent Terra QA passes 137 runtime/contract cases, 37 final presentation
  and public-bundle component cases, 12 regression-runner cases and compiler
  typecheck. A 36/37 attempt exposed one test readiness race; the test now awaits
  visible queue data without weakening count, touch size or action-position
  assertions. The failed attempt remains preserved; the exact combined command
  then passes 37/37 with all other source hashes unchanged.
- Root and QA inspect fully intercepted component captures at 390, 768 and 1440,
  plus retained conflict drafts, uncertain recovery, dark empty state and multiple
  resolution reports. The UI detector's side-border warning is retained as
  deliberate report/cancellation state emphasis after visual inspection.
- Earlier unchanged evidence remains applicable: 185 adjacent database/Task/
  Inventory cases, ten exact generated Prisma `validate` results, and eleven-row
  historical fresh/immutable output parity. These are not new QA reruns.

Detailed attempt logs and frozen identities are under
`generated/.work-orders-integrated-review/` and
`generated/.work-orders-integrated-qa/`. The source review's `scoped-recheck.md`
and QA's `scoped-timing-recheck.md` are the final scoped results. Earlier failures
and original manifests are retained. Original Task 1 baseline SHA-256 remains
`6a2b1429f43ec358eea69e43085386ea0925f731be3b8fbe1e174afcc63b3b81`.

## Open product outcomes

Actual PostgreSQL concurrency/restart and the complete ordinary consumer journey
remain unverified. A policy-rejected local startup is not retried through another
tool or command. Definition registration, automatic entry, case-source integration
and combined definition regression subsequently close within source scope in the
[Task 4 checkpoint](definition-consumer-source.md). Actual execution remains open.
Hosted availability, real identity,
cross-schema upgrades and ordinary-user success remain open Goal outcomes. No
Work Orders runtime/delivery count, main integration or release is promoted here.
