# PM Ledger — Post-v0.1 Local Restaurant Readiness

Design authority:
`docs/superpowers/specs/2026-08-31-post-v0.1-local-restaurant-readiness-design.md`.

Accepted governance decisions:
`docs/adr/adr-0024-post-v0.1-local-operability-profile.md`,
`docs/adr/adr-0025-explicit-pnpm-script-invocation-for-local-doctor.md`, and
`docs/adr/adr-0026-ci-dependency-cache-authority.md`, and
`docs/adr/adr-0027-repository-format-baseline.md`.

Branch: `codex/post-v0.1-local-readiness`.

Iteration states are controlled by PM only:

```text
planned -> implementing -> ready_for_qa -> reviewed -> accepted
```

## Locked outcome

A clean supported Node 22 checkout exposes one safe preflight and one
repeatable local acceptance path that proves the existing Home curated-template
entry, editable Restaurant dual surfaces, immutable Publish and Compilation,
verification, customer ordering, merchant fulfilment, preview, and cleanup.

The guarded real-model Describe path remains an environment-only release gate.
This iteration does not change Graph, API, lifecycle, compiler, generated
runtime, capability, provider, security, or Compose topology contracts.

## Contract ownership

- Product scope and task-state owner: `pm`.
- Operability contract owner: `platform/tech-lead`.
- Acceptance implementation owner: `integration`.
- Test-evidence owner: `qa`.
- Contract status: `accepted` through the founder's explicit ADR-0024,
  ADR-0025, ADR-0026, ADR-0027, ADR-0028, and ADR-0029 acceptances on
  2026-09-01; existing product contracts remain `frozen/unchanged`.
- Future Tech Lead ADRs use the founder's 2026-09-01 standing independent-agent
  review authorization recorded in `docs/tech-governance.md`; qualifying
  reviews continue execution without another founder interruption, while any
  non-qualifying or material-risk decision stops or escalates.
- Contract artifact:
  `docs/superpowers/specs/2026-08-31-post-v0.1-local-restaurant-readiness-design.md`.

PM authorizes only the task marked `implementing` and only its exact allowed
paths. The founder's 2026-09-02 scope decision parks R0 independently and
authorizes U3 without consuming or resolving R0. Generated templates, shared
Graph/API contracts, Compose topology, migrations, and the end-to-end smoke
path remain serialized integration work.

## D0 — Freeze design and governance decision

State: `accepted`.

Owner: `pm` and `tech-lead`.

Allowed paths:

- `docs/superpowers/specs/2026-08-31-post-v0.1-local-restaurant-readiness-design.md`
- `docs/superpowers/ledgers/2026-08-31-post-v0.1-local-restaurant-readiness.md`
- `docs/adr/adr-0024-post-v0.1-local-operability-profile.md`
- `docs/project-status.md`
- `docs/superpowers/plans/2026-09-01-post-v0.1-local-restaurant-readiness.md`

Acceptance evidence:

- target cohort, outcome, exclusions, measurable gates, and branch are explicit;
- current Golden profile and all unchanged product contracts are explicit;
- ADR-0024 recommends keep, experiment, migrate, or reject;
- founder explicitly accepts or rejects ADR-0024;
- PM records the decision before U1 begins.

Acceptance recorded: 2026-09-01. The founder explicitly accepted ADR-0024 and
directed continued execution under a long-running goal. The remote branch-asset
tags were reconciled without treating their earlier creation as ADR acceptance.

## U1 — Supported toolchain doctor

State: `accepted`.

Owner: `platform`.

Allowed paths:

- `.node-version`
- `package.json`
- `scripts/doctor.mjs`
- `scripts/doctor.test.mjs`

Acceptance evidence:

- focused RED/GREEN covers Node, pnpm, Git, Docker, Compose, `.env` names,
  repository root, bounded remediation, and secret-safe output;
- no dependency or lockfile change;
- exact Node `22.11.0` and pnpm `9.0.0` toolchain pass.

Authorized by: accepted ADR-0024 and accepted D0. U1 is the sole implementation
writer until its focused evidence and review are recorded.

Historical collision evidence: on the accepted Node `22.11.0` and pnpm
`9.0.0` toolchain, obsolete `pnpm doctor` resolves pnpm's built-in command and
exits zero without invoking the Archeform package script. The accepted
`pnpm run doctor` form invokes the intended local doctor and fails closed when
the Docker Server is unavailable. The founder accepted ADR-0025 on 2026-09-01;
the supported commands are now exactly `pnpm run doctor` and
`pnpm run doctor:toolchain`. Script keys and mappings remain unchanged.

Acceptance recorded: 2026-09-01. With PATH prefixed by `C:\\nvm4w\\nodejs`,
Node `v22.11.0` and pnpm `9.0.0` were active. `node --test
scripts/doctor.test.mjs` passed 35/35; `pnpm run doctor:toolchain` and
`pnpm run doctor` both passed; and `node scripts/doctor.mjs unknown` returned
the bounded non-zero usage result. The exact `.node-version` byte check,
focused Prettier check, `git diff --check`, and `git diff --exit-code --
pnpm-lock.yaml` all passed. The active-command scan found zero obsolete
shorthand matches; the safe sensitive-pattern scan found zero likely
credential/private-key patterns; and current delivery-path containment found
only `.node-version`, `package.json`, `scripts/doctor.mjs`, and
`scripts/doctor.test.mjs`. Docker server `29.6.2` passed without creating
Compose resources. Independent review previously found P0/P1/P2 `0/0/0`.

U1 acceptance authorizes U2 as the sole `implementing` task. U3 remains
`planned` and serialized behind U2; this record does not authorize U3 work.

## U2 — Credential-free CI

State: `accepted`.

Owner: `platform`.

Allowed paths:

- `.github/workflows/ci.yml`
- `docs/third-party-sources.md`

Acceptance evidence:

- official actions are pinned to full commit SHAs;
- the new provenance record binds each action tag to its official peeled commit
  and license;
- permissions are read-only;
- Node `22.11.0` and current `22.x` run deterministic frozen-install,
  formatting, typecheck, test, build, notice, and source-study gates;
- CI has no secrets, Docker, provider call, publish, or deployment authority.
- the pinned setup-node action selects Node only; CI neither restores nor saves
  dependency-cache state and adds no substitute cache action.

Authorized by: accepted U1, the frozen explicit Doctor command contract, and
accepted ADR-0026. The founder accepted ADR-0026 on 2026-09-01, resolving the
plan conflict in favor of removing `cache: pnpm` and prohibiting dependency
cache restore or save.

Delivery evidence: commits `5975f79a` and `12458713` are pushed. Independent
review found specification compliance, quality Approved, and P0/P1/P2
`0/0/0`. GitHub run `33510492402` proved both Node lanes passed checkout,
setup, exact pnpm activation, frozen install, toolchain Doctor, and Prisma
generation, then exposed the inherited root formatting baseline. After F0 and
the bounded Compiler fixture correction were delivered, replacement GitHub run
`33530986071` at commit `140f8646` passed every configured step in both Node
`22.11.0` and current `22.x` lanes. U2 is accepted on that remote evidence.

## F0 — Repository format baseline

State: `accepted`.

Owner: `platform`, with independent review and controller-only delivery
complete.

Allowed paths:

- `.prettierignore`;
- exactly the 57 tracked formatting paths frozen in Task 3 of the active
  implementation plan;
- the byte-integrity-protected
  `packages/capabilities/assets/core.audit/1.0.2/tests/contract.json`, which
  must remain absent from the tracked diff and retain its accepted raw bytes;
- four Windows normalization-only YAML working copies that must remain absent
  from the tracked diff;
- this ledger, the active plan, ADR-0027, ADR-0028, ADR-0029,
  `docs/tech-governance.md`, and
  `docs/project-status.md` under PM ownership only.

Acceptance evidence:

- RED reproduces 101 tracked local candidates and remote run `33510492402`;
- the partition remains 101 candidates: 39 provenance-protected, one
  byte-integrity-protected, and 61 writable candidates consisting of 57
  tracked formatting changes and four normalization-only paths;
- the 39 copied-source blobs, their provenance evidence, and the protected
  `core.audit` contract evidence remain byte-identical; the protected evidence
  retains SHA-256
  `21335a5e618119a4db05444913cd73a8940ce758df5b42de89b2ac739c94c7bb`;
- the remaining 11 formatted JSON pre/post values are deeply equal;
- focused Graph, capability, compiler, notice, source-study, typecheck, test,
  build, and unchanged-lockfile gates pass; Prisma generation precedes the root
  typecheck, test, and build gates; the generated notification outbox verifier
  becomes green only after the separate accepted ADR-0029 repair;
- root formatting becomes green without changing its command or weakening CI;
- independent review reports specification compliance and P0/P1 `0/0`;
- a replacement remote CI run passes both Node lanes.

Authorized by: the founder explicitly accepted ADR-0027 and its exact
byte-integrity amendment ADR-0028 on 2026-09-01. ADR-0028 replaces the stale D0
governance CLI/test checks for this migration with direct copied-source blob
equality plus the third-party and source-study verifiers. The 58-path candidate
was independently reviewed as specification compliant with P0/P1/P2 `0/0/0`
and delivered at commit `e0cfaa94`. Controller integrity checks preserved all
39 copied-source blobs, the protected `core.audit` SHA-256, four normalization-
only paths, 11 JSON values, and every required local gate. Replacement GitHub
run `33530986071` passed both complete Node lanes. F0 is accepted.

## V0 — Generated notification outbox verifier delegate repair

State: `accepted`.

Owner: `integration`, with independent ADR and task reviewers and
controller-only delivery.

Allowed paths:

- `scripts/verify-generated-notification-outbox.mjs` for exactly two obsolete
  proof-only Prisma count delegates;
- ADR-0029, this ledger, the active plan, `docs/tech-governance.md`, and
  `docs/project-status.md` under governance ownership only;
- ignored SDD Task 4 brief, report, and review artifacts as evidence only.

Acceptance evidence:

- the founder explicitly accepted ADR-0029 on 2026-09-01;
- separate qualified reviewer `/root/review_adr_0029` reported
  `APPROVED_FOR_STANDING_ACCEPTANCE: yes`, specification compliance, and
  P0/P1/P2 `0/0/0` on 2026-09-01;
- RED proves exactly two obsolete `prisma.notificationOutbox.count` accesses
  fail generated API typecheck with `TS2339`, while the accepted generated model
  and store use `Factory_NotificationOutbox` /
  `factory_NotificationOutbox`;
- GREEN changes only those two delegates and preserves every predicate,
  runtime assertion, bounded output, safe failure, Compose identity, and
  teardown behavior;
- generated notification outbox verification, accepted ADR-0028 integrity,
  focused package gates, Prisma generation, root format/typecheck/test/build,
  cleanup, and unchanged-contract checks pass;
- independent task review reports specification compliance and P0/P1 `0/0`;
  controller delivers a dedicated non-force commit without staging the frozen
  58-path F0 candidate.

Authorized by: the founder's explicit ADR-0029 acceptance on 2026-09-01 and the
clean independent ADR review required by the same founder directive.

Acceptance recorded: 2026-09-01. Engineer `/root/implement_outbox_verifier_delegate`
captured the TS2339 RED and changed only the two authorized delegates. The
generated Compose proof emitted the exact bounded six-field summary and left no
Compose resource. Independent task reviewer `/root/review_task4_outbox_verifier`
reported specification compliant, quality Approved, P0/P1/P2 `0/0/0`, and
READY_FOR_DELIVERY. Controller verification passed the generated verifier,
Graph 661/661, capabilities 384/384, compiler 614/614, third-party,
source-study, Prisma generation, root format/typecheck/test/build, diff,
cleanup, and exact 59-total/58-frozen manifest gates. Commit `0bed0d3e` was
pushed without force with local/upstream `0/0`. V0 subsequently enabled the F0
delivery and replacement CI evidence recorded above.

## R0 — Candidate conformance durable-winner convergence

State: `blocked/parked`.

Owner: `platform/tech-lead`; there is no active implementation writer.

Allowed implementation paths:

- `packages/external-intake/src/candidates.ts`;
- `packages/external-intake/src/store.ts`;
- `packages/external-intake/test/candidates.test.ts`;
- `packages/external-intake/test/store.test.ts`.

Governance evidence:

- remote run `33529198551` exposed the strict-access TOCTOU ordering;
- QA reproduced a second immutable-store partial-publication ordering in 1 of
  8 concurrent Node `22.11.0` runs; serial runs and later green CI do not close
  a scheduler-dependent production defect;
- the first ADR reviewer `/root/review_adr_0030` reported P0/P1/P2 `0/1/0`
  because sibling crash-left staging could disable strict Candidate listing;
- the Tech Lead revised staging to the isolated, verified, same-filesystem
  `<quarantine-root>/.immutable-staging` namespace and added two crash-phase
  isolation gates;
- the separate final reviewer `/root/review_adr_0030_revision` reported
  decision-gate compliance, P0/P1/P2 `0/0/0`, and
  `APPROVED_FOR_STANDING_ACCEPTANCE: yes` on 2026-09-02;
- PM accepts ADR-0030 under the founder's standing authorization. The slice is
  bounded, reversible, local-only, and authorizes no release, Product Publish,
  cloud, provider, paid resource, credential, destructive migration, or
  security weakening.

Architecture blocker: the supported Windows/Node path-based hard-link API
cannot bind publication atomically to an already verified directory handle.
Independent review demonstrated a junction replacement between containment
check and `linkSync` that creates an outside file before any post-link check.
That violates the current `docs/threat-model.md` pre-write containment rule.
ADR-0031 remains unaccepted and R0 must not be described as resolved. The two
focused RED tests and the untracked ADR-0031 remain preserved, unmodified,
unstaged, and uncommitted in the original workspace. No ADR-0032, native
helper, service, container, database CAS, or threat-model weakening is
authorized. R0 is explicitly decoupled from U3 by the founder and remains
parked until a separate future scope decision.

## U3 — Real Home template product acceptance

State: `accepted`.

Owner: `integration`, with independent review and controller verification
complete.

Allowed paths:

- `scripts/local-product-acceptance.mjs`
- `scripts/local-product-acceptance.test.mjs`
- `e2e/restaurant-template-acceptance.spec.ts`
- `package.json`
- `README.md`
- `docs/acceptance/post-v0.1-local-restaurant-readiness.md`

Acceptance evidence:

- focused runner RED/GREEN covers unique identity, safe generated inputs,
  command failure, interruption, cleanup failure, minimum Compose version,
  resolved loopback bindings, and non-zero accessibility rejection; 21/22 tests
  pass on Windows with the Unix-only process-group test skipped;
- Docker Compose `2.24.4` is the enforced minimum for the acceptance-only
  `!override` layer, and resolved configuration must prove exactly one
  `127.0.0.1` TCP binding for PostgreSQL, Redis, Control Plane, and Workbench
  before ports are released or infrastructure starts;
- the real Home template entry reaches an editable V3 Restaurant Draft;
- Publish, Compile, Verify, the live V3 customer order and customer-principal
  denial boundaries, generated customer/merchant/shared-state verification,
  dual-surface preview, desktop/narrow accessibility, and cleanup pass;
- no model credential is required and fixture mode is rejected;
- the supported Node `22.11.0`, pnpm `9.0.0`, and Docker Compose `5.3.1` run
  `factory-local-0caab8dec268bafa7499e5087c8762ba2dd7` passed all 11 named
  steps, four zero Axe counts, and four zero cleanup counts;
- the Published Revision digest is
  `sha256:7ed436226966027334c0626554efe15d75af589c1b65f8b0910cf9c5f07941ac`
  and the generated Compose artifact digest is
  `sha256:c17730ee23c6d3354dfbf870c9e9caa16506d86da787c028f27ab8e6a42a4e82`;
- exactly zero run-owned resources remain;
- independent reviewer `/root/review_u3_task5` found the initial loopback and
  Axe-boundary gaps, accepted both focused fixes, accepted the explicit Compose
  minimum, reported P0/P1/P2 `0/0/0`, and returned
  `APPROVED_FOR_U3_ACCEPTANCE: yes`.

Static isolation gates:

- the U3 changed-path manifest must not contain
  `packages/external-intake`;
- U3 implementation, test, E2E, package, README, and acceptance-document
  sources must contain no reference to `@factory/external-intake`,
  `packages/external-intake`, Candidate registry, ADR-0030, or ADR-0031;
- U3 must not import, call, modify, or depend on External Intake or either
  unfinished Candidate ADR.

Authorized by: accepted F0 and U2 plus the founder's explicit 2026-09-02
scope-decoupling decision. R0 remains architecture-blocked/parked and is not a
U3 prerequisite. The accepted implementation consumes the stable Restaurant V3
target. It does not add the archived V1 `/table/:token` or
`/merchant/cashier` behavior, change the shared Compose topology, or alter a
Graph, compiler, provider, security, or tenant contract.

Acceptance recorded: 2026-09-02. The supported local command, safe evidence,
static isolation checks, exact cleanup proof, and final independent review are
green. U3 is accepted and unblocks the controller-only U4 delivery sequence; it
does not authorize `main` integration, Product Publish, repository release, or
cloud deployment.

## U4 — QA, release review, and delivery

State: `implementing`.

Owner: `qa`, `reviewer`, `pm`, and controller in sequence.

Allowed paths:

- `docs/adr/adr-0032-local-acceptance-role-surfaces-and-preview-lease.md` for
  the accepted U4 repair decision;
- `docs/adr/adr-0034-local-acceptance-interruption-cleanup-owner.md` for the
  rejected supervisor-only cleanup-owner decision and its evidence;
- `packages/compiler/src/targets/restaurant-v3/product-target.ts`,
  `packages/compiler/test/restaurant-product-v3-target.test.ts`, and
  `packages/compiler/test/restaurant-runtime.test.ts` for the acceptance-only
  fixed-role services and focused RED/GREEN evidence;
- `scripts/local-product-acceptance.mjs`,
  `scripts/local-product-acceptance.test.mjs`, and
  `e2e/restaurant-template-acceptance.spec.ts` for the runner-owned preview
  handshake/lease, exact cleanup, live role journey, and focused RED/GREEN
  evidence;
- `docs/adr/adr-0033-local-acceptance-preview-profile-activation.md` for the
  accepted local profile-activation prerequisite;
- `apps/compiler-worker/src/preview-runner.ts` and
  `apps/compiler-worker/test/preview-runner.test.ts` for exact, fail-closed
  acceptance-profile activation without changing the public preview API or
  normal preview behavior;
- this ledger and `docs/project-status.md` for PM reconciliation.

Acceptance evidence:

- supported Node 22 focused and relevant package gates are fresh;
- deterministic isolated product acceptance passes with empty cleanup;
- independent task review, QA, and final review report P0/P1 = 0/0;
- accepted task commits are pushed without force;
- local branch tip equals its upstream and the working tree is clean.

Governance repair accepted: ADR-0032 is accepted on 2026-09-02 under the
founder's standing independent-review authorization. Tech Lead
`/root/u4_adr0032` proposed the bounded `experiment`; independent reviewer
`/root/review_adr_0032` first reported P0/P1/P2 `0/4/1`, then confirmed every
finding closed with P0/P1/P2 `0/0/0`, bounded/reversible `yes`, no unresolved
material choice, and `APPROVED_FOR_STANDING_ACCEPTANCE: yes`. PM authorizes one
serialized integration owner to implement only the exact paths above, starting
with focused failing tests. Any shared-contract change or path expansion stops
the repair and returns to PM/Tech Lead.

Release blockers: fresh QA reported P0/P1/P2 `0/1/1` and
`QA_APPROVED_FOR_U4: no`; independent release review reported P0/P1/P2
`0/2/0` and `APPROVED_FOR_U4_RELEASE: no`. The branch must prove the live
customer-to-kitchen fulfilment outcome with observation-only cashier/manager
state, exact per-step verification status, and deterministic cleanup across
all interruption windows before those gates can be rerun.

Unblocked by: accepted U1, U2, U3, and ADR-0032. The next sequence is focused
RED, bounded implementation, focused GREEN, full local acceptance including a
real interruption exercise, task review, QA, release review, PM reconciliation,
then controller delivery. `main` integration and release remain separately
gated.

Profile activation prerequisite accepted: the first implementation feasibility
check stopped with zero edits because the existing worker had no profile
activation channel. ADR-0033 is accepted on 2026-09-02 under the same standing
authorization. Tech Lead `/root/u4_adr0033` proposed the local-only activation
adapter; reviewer `/root/review_adr_0033` first reported P0/P1/P2 `0/1/0`, then
confirmed the cleanup-only recovery repair at P0/P1/P2 `0/0/0`,
bounded/reversible `yes`, material ambiguity `no`, and
`APPROVED_FOR_STANDING_ACCEPTANCE: yes`. PM expands the serialized owner only
to the two compiler-worker paths listed above. Public API, queue, Graph, schema,
stable identifiers, tracked `infra/docker-compose.yml`, manifests, lockfile,
normal preview, cloud, release, and R0 remain frozen.

Interruption topology decision rejected: ADR-0034 is rejected on 2026-09-02
under the founder's standing authorization. Tech Lead
`/root/u4_adr0034_interrupt_supervisor` first proposed a detached
supervisor-only experiment. Reviewer `/root/review_adr0034_interrupt_owner`
reported P0/P1/P2 `0/5/2`; the revised ADR records that a bounded supervisor
cannot safely resolve a preview-start request delayed before server-assigned
identity creation while the current API/queue/schema contracts remain frozen.
The same reviewer confirmed the exact reviewed proposal SHA-256
`001642d5c8d0954afc93ad211a411a4dc230a20d0c4fa0e90d7baf461f4ae163`,
P0/P1/P2 `0/0/0`, bounded/reversible `yes`, material ambiguity `no`, and
`APPROVED_FOR_STANDING_ACCEPTANCE: yes` solely for the reject/no-implementation
decision. PM authorizes no ADR-0034 implementation. The next step is a separate
Tech Lead ADR for a minimal shared preview-intent identity contract; U4,
delivery, and `main` integration remain blocked meanwhile.

## Branch asset hold

`agent/console-next-shadcn` and `codex/commerce-transaction-v1` remain preserved
unchanged and are not iteration inputs. Their exact tips are remotely preserved
by annotated tags `archive/agent-console-next-shadcn-2026-08-31` at `dbf0ba40`
and `archive/commerce-transaction-v1-2026-08-31` at `b481fc78`. The local branch
names remain present. This iteration authorizes neither deletion nor bulk
cherry-pick from either asset.
