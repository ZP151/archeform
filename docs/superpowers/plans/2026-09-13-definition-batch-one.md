# Representative definition batch one

Execute the user-authorized catalogue expansion from source `327eb908`.
Design: `docs/superpowers/specs/2026-09-13-definition-batch-one-design.md`.
Tech Lead disposition: KEEP, existing ADR-0065 V1/family authority suffices.

## Frozen scope and ownership

Admit Publication Review only if all business and presentation checks pass.
Target five current definitions, three runtime families; preserve the historical
four-entry fixture unchanged. No new runtime, Graph, API, package, provider,
security boundary, generated template or product-specific UI implementation.
Root owns Git, services, historical compatibility, CLI regression, integration
and delivery evidence. One catalogue writer owns the JSON row and adapter tests.
A separate test writer owns only the shared approval acceptance helper and new
parameterized batch test files. Writers do not change the frozen data format.

## Task 1: Restore actual acceptance collection

Reproduce the Playwright dependency module-mode failure before services. Trace
the transformation/resolution boundary and fix the smallest test-loader seam.
Require original Approval/Purchase test collection and Task collection to pass.
Do not alter production module or technology contracts to hide a harness issue.

## Task 2: Add a reviewed data-only definition

Start with focused failing tests for explicit five-key membership, distinct
Publication fields/roles, public adapter projection and material exclusions.
Append the reviewed Publication row described in the design. Keep provenance
decision ADR-0065 as the format authority. Do not change the first four entries.
Authored provider-free selection is not evidence of model classification.
Update current-count assertions separately from immutable historical fixtures.

## Task 3: Reuse actual business acceptance

Parameterize the existing correction helper's edited field and select values;
retain its fixed state/version/history/retry assertions. Add one batch runner
and Publication case using the existing Approval shell. Verify create, return,
same-record correction/resubmit/approve, observer history, reload persistence,
required/enum rejection, role denial, stale edits and interrupted action retry.
Record actual generated primary/input/outcome screens at 390/768/1440, inspect
loaded styling/icons and reachable controls. Root launches one isolated Factory
and owns exact project cleanup. Use zero model calls and no external publishing.

## Task 4: Reconcile and deliver

Run focused adapter/data/authoring/historic compatibility and built CLI checks.
Retain the historical fixture bytes, compare by its four recorded keys. Record
six candidate dispositions honestly: Publication admitted only after acceptance;
Training numeric domain; Equipment quantity/arithmetic; Travel ordered dates and
budget; Leave overlap/allowance; Access identity/provisioning. Exact Purchase
renaming is a duplicate control, not the original Equipment requirement.
Update authoring guide, product scorecard, roadmap and PM ledger. One independent
review covers the bounded integration and actual evidence; do not add a new ADR
or full release ceremony per JSON row. Resolve findings, commit, push and verify
remote equality/clean status before completing the goal.

## Actual presentation correction discovered during admission

Attempt1 reached a real generated app but exposed missing visible business
identity. The original data-only scope remains correct for the definition row;
the shared Approval renderer needs a one-time generic title/enum-summary repair
before this admission can complete. ADR-0066 is now accepted through the recorded
independent standing decision; its OWN-002 source boundary is the sole extension
to the initial data-only scope. No per-product renderer is introduced.
Existing first-four fixture bytes remain the abort-on-drift baseline.
Publication retains the existing family hero with optional record media; old
Expense/Purchase row media remains required. Actual screenshots must precede
visual assertions so failures remain reviewable, and acceptance now explicitly
requires two distinguishable visible titles/channels in list and decision history.

## Completion

Tasks 1-3 and the accepted shared presentation extension are complete. Final
actual attempt 5 passes in 4.3 minutes; the generated app is ready in 191,368 ms.
Task 4 independent review passes with no open in-scope P0/P1/P2, and ten exact
runtime projects are clean. Root accepts the bounded five-definition catalogue
and owns the remaining normal branch commit/push. The acceptance record and
scorecard retain all failed attempts, the existing long-ID limitation and the
next numeric-domain business expansion; no hosted maturity claim is made.
